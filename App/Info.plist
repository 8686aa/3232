<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>zh_CN</string>
	<key>CFBundleDisplayName</key>
	<string>GORaDar</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>1.0</string>
	<key>CFBundleVersion</key>
	<string>1</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UILaunchScreen</key>
	<dict/>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>
	<!-- 局域网设备（另一台 iPhone 上的小火箭）要连进来，必须声明用途，
	     否则 iOS 14+ 会直接掐断监听/连接且不报错 -->
	<key>NSLocalNetworkUsageDescription</key>
	<string>需要接受同一局域网内设备的 SOCKS5 连接，才能作为中间人转发流量。</string>
	<!-- 节点地址（ws://…:1082）与雷达服务（http://…:666）都是明文，且 IP 由用户在界面自填，
	     无法预先声明域名白名单，只能整体放开：NSAllowsArbitraryLoads 同时覆盖
	     URLSession 的明文请求（雷达校验、账号登录走的就是它）与内置 WebView 的 http 页面。
	     注意：不要再加 NSAllowsArbitraryLoadsInWebContent / NSAllowsArbitraryLoadsForMedia /
	     NSAllowsLocalNetworking —— iOS 10+ 只要出现这三个键里的任意一个，本键就会被整个忽略，
	     结果是 WebView 能开网页、但 URLSession 被 ATS 静默拦死，雷达校验永远拿不到链接。 -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsArbitraryLoads</key>
		<true/>
	</dict>
</dict>
</plist>
